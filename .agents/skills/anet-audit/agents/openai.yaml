interface:
  display_name: "ANET Audit"
  short_description: "観点1×範囲1でanet-labを定点監査し、指摘を台帳(findings.jsonl)へ蓄積する"
  default_prompt: "$anet-audit を使って、次の観点と範囲でanet-labを監査し台帳を更新してください。"

policy:
  allow_implicit_invocation: false
