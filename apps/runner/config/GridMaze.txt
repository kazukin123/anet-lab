
# ==============================================================================
# 基本設定
# ==============================================================================

app.$ = app.online > P1
#app.$ = app.batchrun > P1

train.seed = 1

train.num_envs = 256
#train.num_envs = 128
#train.num_envs = 10
#train.num_envs = 1

#DefaultDQNAgent.$ = @baseline > @fast > A1 > @bf16 >  A2 > A3
#DefaultDQNAgent.$ = @baseline > @random	 # Random Policy (env 天井の実測用)
DefaultDQNAgent.$ = @baseline > @fast > A1 > A2 > A3
#DefaultDQNAgent.$ = @baseline > @fast > A1 > @bf16 > A2 > A3

DefaultDQNAgent.net.body.$ = net.body.@MLP2


# ==============================================================================
# 実験
# ==============================================================================

#app.run_name = run_{t}

A2.learner.use_rb_prefetch = true

#A2.quantile_mode = none  # qr
#A2.train_policy.policy_type = EpsilonGreedy
#A2.eval_policy.policy_type = EpsilonGreedy


# ==============================================================================
# デバッグ用
# ==============================================================================

#A2.stucker.stack_count = 4
A2.stucker.stack_count = 1

A2.learner.use_per = true

#A2.learner.replay_batch_size = 2

#train.main_runner_type = serial

#A2.learner.n_step = 1

#A2.learner.update_warmup_steps = 10

#app.train_auto_start = false

app.metrics_logger.video_codec = libx264  # h264_nvenc


# ==============================================================================
# ステップ数 依存設定
# ==============================================================================

# --- Run 予算の一点化(値参照=059 D7。online / batchrun 同値のため 1 定数で束ねる) ---
@vars : max_exp_step = 400,000       # 400K

app.online.exp_pause_step  = ${@vars.max_exp_step}
app.batchrun.exp_exit_step = ${@vars.max_exp_step}

A1.train_policy.eps_decay_steps     = 200,000 # 200K
A1.train_policy.uqe_eps_decay_steps = 200,000 # 200K
A1.train_policy.uqe_tau_decay_steps = 400,000 # 400K
A1.learner.per_beta_step = 100,000            # 100K


# ==============================================================================
# Env依存設定
# ==============================================================================


#A1.stucker.use_stacker = true
A1.stucker.stack_count = 1

A1.use_optimistic_target = true         # Q値にtrain_policy（UQE）を反映させるならtrue

A1.train_policy.policy_type = UQE
A1.train_policy.uqe_eps_start = 0.60
A1.train_policy.uqe_eps_end = 0.05
A1.train_policy.uqe_tau_start = 0.95  # 序盤は超楽観的（攻めの姿勢）
A1.train_policy.uqe_tau_end = 0.85    # 終盤でもかなり楽観的（攻めの姿勢）
A1.train_policy.uqe_use_tail_mean = true

A1.eval_policy.policy_type = UQE

A1.target_policy.uqe_tau_start = 0.85
#A1.target_policy.uqe_use_tail_mean = true

A1.learner.alpha = 5e-5         # ★
A1.learner.gamma = 0.995        # ★
A1.learner.n_step = 3           # ★
A1.learner.replay_ratio = 1.0
A1.learner.replay_capacity = 524,288
A1.learner.replay_batch_size = 128            # 256 学習が重めなのので少し下げる
A1.learner.grad_clip_tau = 0.5                # ★厳しめ（報酬を変えたら要調整）
A1.learner.update_warmup_steps = 2,560        # 256(replay-sample) * 10step
#A1.learner.per_alpha = 0.6                   # デフォルト 0.6
#A1.learner.per_beta_start = 0.4  # 0.6       # デフォルト 0.4
A1.learner.per_prio_clip_value = 50           # 30
A1.learner.use_per_prio_clip = true           # 優先度をクリッピングするか

A1.reward_scaler.use_dynamic_scaling = true
A1.reward_scaler.use_auto_post_scale = true
#A1.reward_scaler.constant_scale = 0.001       # ★（報酬値を変えたら要調整）
A1.reward_scaler.use_clipping = false
A1.reward_scaler.clip_range = 10.0

A1.network.soft_update_tau = 0.005                 # 0.001 高め


# Random Policy プロファイル(@random)は agent.txt の共通定義を参照。

# ==============================================================================
# MLP
# ==============================================================================

#  904 (4 + 30x30) x 4stack ≒ 3600次元
#  104 (4 + 10x10) x 4stack ＝  416次元

net.block.FC1.linear.out_features = 1024
net.block.FC2.linear.out_features = 512

#net.block.FC1.linear.out_features = 2048
#net.block.FC2.linear.out_features = 1024

net.block.FC1.init.mode = xavier
net.block.FC2.init.mode = xavier

DefaultDQNAgent.net.branch.[main_feature].bind = vector
DefaultDQNAgent.net.branch.[main_feature].structure = Flatten > MLP_FC1 > ReLU > MLP_FC2 > ReLU
DefaultDQNAgent.net.body.output.[features] = main_feature


# ==============================================================================
# scalar metrics
# ==============================================================================

metrics.scalar.$ = metrics.scalar.@baseline > M1

#metrics.scalar.@baseline.[21_eval/03_target_reward_ema] = mean.episode_return $runner @session_end $eval.[eval1] $ema ema_alpha:0.0002  # 変動が大きいのでema_alphaを小さく
#metrics.scalar.@baseline.[21_eval/04_policy_reward_ema] = mean.episode_return $runner @session_end $eval.[eval2] $ema ema_alpha:0.0002  # 変動が大きいのでema_alphaを小さく

#metrics.scalar.@baseline.[37_agent_qtd/22_grad_norm_ema] = grad_norm @learn interval:10 $ema clip:10.0 # 初期の変動をclip
#metrics.scalar.[90_perf/82_exp_step] = exp_step @train $runner interval:100
metrics.scalar.[32_agent_base/04_uqe_tau_target] = $agent target_policy.uqe_tau @learn $exp_step

#metrics.scalar.[22_eval/01_test1_reward]     = mean.episode_return $runner @session_end $eval.[test1] clip:3000
#metrics.scalar.[22_eval/02_test2_reward]     = mean.episode_return $runner @session_end $eval.[test2] clip:3000
#metrics.scalar.[22_eval/03_test1_reward_ema] = mean.episode_return $runner @session_end $eval.[test1] $ema ema_alpha:0.001 clip:1000
#metrics.scalar.[22_eval/04_test2_reward_ema] = mean.episode_return $runner @session_end $eval.[test2] $ema ema_alpha:0.001 clip:1000


metrics.scalar.[42_env/51_wall_collisions_max]  = $env max.wall_collisions @train
metrics.scalar.[42_env/61_is_success_mean]      = $env mean.is_success @train
metrics.scalar.[42_env/62_is_hole_mean]         = $env mean.is_hole @train
metrics.scalar.[42_env/63_wall_collisions_mean] = $env mean.wall_collisions @train

metrics.scalar.[42_env/51_wall_collisions_max_ema]  = $env max.wall_collisions @train $ema ema_alpha:0.001
metrics.scalar.[42_env/61_is_success_mean_ema]      = $env mean.is_success @train $ema ema_alpha:0.005
metrics.scalar.[42_env/62_is_hole_mean_ema]         = $env mean.is_hole @train $ema ema_alpha:0.005
metrics.scalar.[42_env/63_wall_collisions_mean_ema] = $env mean.wall_collisions @train $ema ema_alpha:0.001


metrics.scalar.[42_env/00_ep_step_mean]  = $env mean.episode_len @train
metrics.scalar.[42_env/00_ep_step_mean_ema]  = $env mean.episode_len @train $ema ema_alpha:0.001


# ==============================================================================
# image metrics
# ==============================================================================

metrics.image.interval = 0   # 出力全無効
#metrics.image.interval = 1   # 毎ステップ出力
#metrics.image.interval = 10   # 通常運用

image.phm.@per-prio.heatmap.width = 10
image.phm.@per-prio.heatmap.height = 10

#metrics.image.[43_agent_img/52_per_hm_prio_01].$ = image.phm.@per-prio > image.phm.@s01
#metrics.image.[45_agent_img/05_shm_01_maxq_01].$ = image.shm.@qmax > image.shm.01



# ==============================================================================
# app
# ==============================================================================

#app.online.eval_panel.step_per_frame = 5

app.online.eval_panel.auto_start = false
app.batchrun.eval_panel.auto_start = false

QValuePanel.row_height = 24

app.online.train_panel.fps = 30


# ==============================================================================
# GridMazeEnv
# ==============================================================================

env.class_id = GridMazeEnv


GridMazeEnv.width = 5
GridMazeEnv.height = 5
GridMazeEnv.seed = 6545537233705677626
GridMazeEnv.randomize_seed = false

GridMazeEnv.wall_density = 0.2
GridMazeEnv.hole_density = 0.1

GridMazeEnv.init_pos_x = 0
GridMazeEnv.init_pos_y = 0
GridMazeEnv.goal_pos_x = 4
GridMazeEnv.goal_pos_y = 4

GridMazeEnv.reward_goal = 1.0
GridMazeEnv.reward_hole = -1.0
GridMazeEnv.reward_wall = -0.1
GridMazeEnv.reward_step = -0.01

GridMazeEnv.max_steps = 100

# --- Eval設定 ---

#train.eval_schedule.[eval1].interval = 0
#train.eval_schedule.[eval2].interval = 0
#train.eval_schedule.[eval1].interval = 50
#train.eval_schedule.[eval2].interval = 50
train.eval_schedule.[eval1].interval = 10
train.eval_schedule.[eval2].interval = 10
