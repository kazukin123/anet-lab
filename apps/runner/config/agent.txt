
DefaultDQNAgent.$ = DefaultDQNAgent.baseline
RainbowAgent.$ = RainbowAgent.baseline
DQNAgent.$ = DQNAgent.baseline

# ---- DefaultDQNAgent


## MEMO
# U = N * RR / B
# RR = U * B / N

## MEMO
# 報酬は暴れてないか？ → reward_scaler
# 設定単純な反射神経ゲーか、戦略ゲーか？ → gamma （長期戦略が必要な場合はgammaを上げる）
# 設定どれくらい複雑か？ → Network Size設定　（大きすぎると過学習気味になる。単純なモデルなら中間層 64とかに下げる）


DefaultDQNAgent.baseline.head_init.manual_gain = 0.0
DefaultDQNAgent.baseline.head_init.mode = 1
DefaultDQNAgent.baseline.network.soft_update_tau = 0.001      # 1.0f 0.004f  0.01f 0.005f;
DefaultDQNAgent.baseline.stucker.use_stacker = true
DefaultDQNAgent.baseline.stucker.stack_count = 4
DefaultDQNAgent.baseline.action_policy.policy_type = 1        # 0=EpsilonGreedy 1=UQE 2=TomsonSampling
DefaultDQNAgent.baseline.action_policy.eps_max = 1.00               # 指数減衰 for EpsilonGreedy  
DefaultDQNAgent.baseline.action_policy.eps_min = 0.05               # 0.1f 0.05f for EpsilonGreedy
DefaultDQNAgent.baseline.action_policy.eps_decay_step = 10,000,000  # 10M for EpsilonGreedy
DefaultDQNAgent.baseline.action_policy.uqe_tau_max = 0.9            # for UQE
DefaultDQNAgent.baseline.action_policy.uqe_tau_min = 0.5            # for UQE
DefaultDQNAgent.baseline.action_policy.uqe_tau_decay_step = 10,000,000  # 10M for UQE
DefaultDQNAgent.baseline.action_policy.uqe_eps_max = 0.05
DefaultDQNAgent.baseline.action_policy.uqe_eps_min = 0.05
DefaultDQNAgent.baseline.action_policy.uqe_eps_decay_step = 0       # 0=max固定
DefaultDQNAgent.baseline.action_policy.uqe_use_tail_mean = false    # for UQE&TomsonSampling
DefaultDQNAgent.baseline.learner.alpha = 5e-4                 # 学習率 1e-3 3e-3 1e-4 1e-4 3e-4 5e-4
DefaultDQNAgent.baseline.learner.gamma = 0.99                 # ★ 0.99 0.995  γが高いほど「長期安定」を目指す
DefaultDQNAgent.baseline.learner.adam_eps = 1e-5              # 小さな勾配の変化に敏感になりすぎるのを防ぎ、学習をマイルドに
DefaultDQNAgent.baseline.learner.use_grad_clip = true         # ★
DefaultDQNAgent.baseline.learner.grad_clip_tau = 5            # ★10 25 40 1f 5f 10f
DefaultDQNAgent.baseline.learner.use_td_clip = false          # 
DefaultDQNAgent.baseline.learner.td_clip_value = 30.0         # 4.0 20 30
DefaultDQNAgent.baseline.learner.replay_capacity = 2560000    # batch_size=256向け。
DefaultDQNAgent.baseline.learner.replay_batch_size = 1024     # 128 256 1024 大きいと実時間は早くなるが局所解に陥りやすくなる
DefaultDQNAgent.baseline.learner.update_warmup_steps = 8192  # 1000　exp_step単位。replay_batch_size * 32 もしくは batch_size * エピソードの平均長、どちらか大きい方
DefaultDQNAgent.baseline.learner.update_interval = 1         # 環境何ステップ毎にUpdateするか。replay_rationが正なら使われない
DefaultDQNAgent.baseline.learner.replay_ratio = 4            # 環境1ステップあたり平均何回のUpdateするか。負数ではupdate_interval側を使う
DefaultDQNAgent.baseline.learner.n_step = 3
DefaultDQNAgent.baseline.learner.per_alpha = 0.6             # 優先度の反映度合い (0.0:ランダム選択, 1.0:優先度のみ考慮して選択)
DefaultDQNAgent.baseline.learner.per_beta_start = 0.4        # 「beta」の初期値 β：同じ行動を選び続けないように優先度補正する強度
DefaultDQNAgent.baseline.learner.per_beta_end = 1.0f         # 「beta」の収束値 β：同じ行動を選び続けないように優先度補正する強度
DefaultDQNAgent.baseline.learner.per_beta_step = 10,000,000  # 10M betaを収束値まで線形変化させるexpステップ数（初期は当てにならないので補正を弱める）
DefaultDQNAgent.baseline.learner.per_eps = 1e-6f             # 優先度加算用微小値
DefaultDQNAgent.baseline.learner.per_initial_priority = 1.0  # 新規データの初期優先度
DefaultDQNAgent.baseline.learner.use_per_prio_clip = true    # 優先度をクリッピングするか
DefaultDQNAgent.baseline.learner.per_prio_clip_value = 20.0  # 優先度の上限値
DefaultDQNAgent.baseline.learner.quantile_huber_kappa = 1.0  # Huber Loss の閾値 kappa
DefaultDQNAgent.baseline.learner.use_double_dqn = true       # Double DQN 有効化フラグ（trueで有効）
DefaultDQNAgent.baseline.learner.use_n_step = true
DefaultDQNAgent.baseline.learner.use_per = true
DefaultDQNAgent.baseline.reward_scaler.use_clipping = false
DefaultDQNAgent.baseline.reward_scaler.clip_range = 10
DefaultDQNAgent.baseline.reward_scaler.constant_scale = 0.01
DefaultDQNAgent.baseline.reward_scaler.use_dynamic_scaling = true
DefaultDQNAgent.baseline.reward_scaler.epsilon = 1e-8
DefaultDQNAgent.baseline.reward_scaler.use_auto_post_scale = true   # gammaを使って自動設定するか
DefaultDQNAgent.baseline.reward_scaler.reference_q_std = 30.0       # 自動設定時のQ値分散目標スケール (推奨: 3.0～50.0)
DefaultDQNAgent.baseline.reward_scaler.manual_post_scale = 1.0      # use_auto_post_scale=true では使われない
DefaultDQNAgent.baseline.obs_norm.pass_through = true               # ★LunarLanderにおいては効果を確認出来ていない(逆に悪化)のでtrue
#DefaultDQNAgent.baseline.obs_norm.constant_mean = 0 0 0 0 0 0 0 0
#DefaultDQNAgent.baseline.obs_norm.constant_std = 1.273 1.275 1.39 2.0 1.814 4.365 1.013 1.016
DefaultDQNAgent.baseline.obs_norm.use_clipping = false
DefaultDQNAgent.baseline.obs_norm.clip_range = 10.0
DefaultDQNAgent.baseline.obs_norm.use_dynamic_scaling = true        # 
DefaultDQNAgent.baseline.obs_norm.use_centering = false             # ★LunarLanderにおいては零点が意味を持つのでfalseのほうが良い
DefaultDQNAgent.baseline.obs_norm.epsilon = 1                       # 1 0.1 小さいとContractが異常値扱いで弾かれるので1.0推奨
DefaultDQNAgent.baseline.obs_norm.use_robust_update = true
DefaultDQNAgent.baseline.obs_norm.robust_warmup_count = 200
DefaultDQNAgent.baseline.obs_norm.robust_std_threshold = 5.0
DefaultDQNAgent.baseline.obs_norm.post_process_type = 1             # 0=none 1=SymLog 2=Tanh 3=Softsign
DefaultDQNAgent.baseline.obs_norm.post_process_threshold = 10.0
DefaultDQNAgent.baseline.use_dueling_net = true       #  
DefaultDQNAgent.baseline.num_quantiles = 51           # 分位数 N (デフォルト51)
DefaultDQNAgent.baseline.use_qr = true                # QR-DQN 有効化フラグ

# ---- RainbowAgent

RainbowAgent.baseline.head_init.manual_gain = 0.0
RainbowAgent.baseline.head_init.mode = 1
RainbowAgent.baseline.network.soft_update_tau = 0.001     # 1.0f 0.004f  0.01f 0.005f;
RainbowAgent.baseline.action_policy.eps_max = 1.00              # 指数減衰  
RainbowAgent.baseline.action_policy.eps_min = 0.05              # 0.1f 0.05f
RainbowAgent.baseline.action_policy.eps_decay_step = 10,000,000    # 10M
RainbowAgent.baseline.learner.alpha = 5e-4                # 学習率 1e-3 3e-3 1e-4 1e-4 3e-4 5e-4
RainbowAgent.baseline.learner.gamma = 0.99                # ★ 0.99 0.995  γが高いほど「長期安定」を目指す
RainbowAgent.baseline.learner.use_grad_clip = true        # ★
RainbowAgent.baseline.learner.grad_clip_tau = 30          # ★10 25 40 1f 5f 10f
RainbowAgent.baseline.learner.use_td_clip = false         # 
RainbowAgent.baseline.learner.td_clip_value = 30.0        # 4.0 20 30
RainbowAgent.baseline.learner.replay_capacity = 100,000   #  10000 30000
RainbowAgent.baseline.learner.replay_batch_size = 128     #
RainbowAgent.baseline.learner.update_warmup_steps = 1000  # 1000
RainbowAgent.baseline.learner.update_interval = 5         #
RainbowAgent.baseline.learner.n_step = 3
RainbowAgent.baseline.learner.per_alpha = 0.6             # 優先度の反映度合い (0:uniform, 1:full)
RainbowAgent.baseline.learner.per_beta_start = 0.4        # IS重みの補正度合い「beta」の初期値
RainbowAgent.baseline.learner.per_beta_end = 1.0          # IS重みの補正度合い「beta」の収束値
RainbowAgent.baseline.learner.per_beta_step = 20000       # 20K betaを収束値まで線形変化させるステップ数
RainbowAgent.baseline.learner.per_eps = 1e-6f             # 優先度加算用微小値
RainbowAgent.baseline.learner.per_initial_priority = 1.0  # 新規データの初期優先度
RainbowAgent.baseline.learner.use_per_prio_clip = true    # 優先度をクリッピングするか
RainbowAgent.baseline.learner.per_prio_clip_value = 20.0  # 優先度の上限値
RainbowAgent.baseline.learner.quantile_huber_kappa = 1.0  # Huber Loss の閾値 kappa
RainbowAgent.baseline.learner.use_double_dqn = true       # Double DQN 有効化フラグ（trueで有効）
RainbowAgent.baseline.learner.use_n_step = true
RainbowAgent.baseline.learner.use_per = true
RainbowAgent.baseline.use_dueling_net = true       #  
RainbowAgent.baseline.num_quantiles = 51           # 分位数 N (デフォルト51)
RainbowAgent.baseline.use_qr = true                # QR-DQN 有効化フラグ

# ---- DQNAgent

DQNAgent.20251127-032759.nn_init_mode=0         # // 0=default、1=XavierUniform、2=HeNormal
DQNAgent.20251127-032759.alpha=5e-4             # ★学習率 1e-3 3e-3 1e-4 1e-4 3e-4 5e-4
DQNAgent.20251127-032759.gamma=0.99             # 0.99f; 0.995f      γが高いほど「長期安定」を目指す
DQNAgent.20251127-032759.eps_max=1.00           # 指数減衰  
DQNAgent.20251127-032759.eps_min=0.05           # 0.1f 0.05f
DQNAgent.20251127-032759.eps_decay_step=100000  # 100K
DQNAgent.20251127-032759.softupdate_tau=0.001   # 1.0f 0.004f  0.01f 0.005f;
DQNAgent.20251127-032759.hardupdate_interval=-1 #  -1 5000; //200 500 1000
DQNAgent.20251127-032759.use_grad_clip=true     #
DQNAgent.20251127-032759.grad_clip_tau=25       # 10 25 40 1f 5f 10f
DQNAgent.20251127-032759.use_td_clip=true       #
DQNAgent.20251127-032759.td_clip_value=3.0      # 4.0
DQNAgent.20251127-032759.eps_zero_step=-1       # 120000;
DQNAgent.20251127-032759.use_double_dqn=true    # Double DQN 有効化フラグ（trueで有効）
DQNAgent.20251127-032759.use_replay_buffer=true #
DQNAgent.20251127-032759.replay_capacity=10000
DQNAgent.20251127-032759.replay_batch_size=128
DQNAgent.20251127-032759.replay_warmup_steps=10000     #
DQNAgent.20251127-032759.replay_update_interval=10     #
DQNAgent.20251127-032759.use_as_dqn = true             # Adaptive Stabilized DQN (AS-DQN)
DQNAgent.20251127-032759.eps_gain = 0.5                # ■
DQNAgent.20251127-032759.q_z_threshold = 2.6           # 2.0 1.5-2.0 3.0 z-score 崩壊判定閾値
DQNAgent.20251127-032759.q_cv_threshold = 0.35         # 0.45 0.3 0.25-0.35 0.5 CV 崩壊判定閾値
DQNAgent.20251127-032759.q_niqr_threshold = 0.8        # 0.65 0.5 0.4-0.5 0.6 NIQR 崩壊判定閾値
DQNAgent.20251127-032759.eps_boost_max = 2.0           #ε ブースト上限倍率
DQNAgent.20251127-032759.eps_boost_half_life_hit = 300 # 崩壊中、ε ブーストが2倍になるまでのstep数
DQNAgent.20251127-032759.eps_boost_half_life_recover = 3000 # 安定後、ε ブーストが半減するまでのstep数
DQNAgent.20251127-032759.eps_reheat_floor = 0.50       #  0.25 0.2 ε再加熱の最大値（崩壊時もこの値以上にはならない）
DQNAgent.20251127-032759.eps_reheat_half_life = 2000   # ε再加熱の回復半減step数
DQNAgent.20251127-032759.tau_max = 0.001               # τの上限
DQNAgent.20251127-032759.tau_min = 0.0001              # 0.0003 τの下限
DQNAgent.20251127-032759.tau_half_life_hit = 1200      # 300 崩壊中、τが半減するまでのstep数
DQNAgent.20251127-032759.tau_half_life_recover = 2000  # 1000 安定後、τが2倍になるまでのstep数
DQNAgent.20251127-032759.tau_recover_delay = 1000      # このステップ期間安定していたら回復開始
DQNAgent.20251127-032759.act_bias_threshold = 0.35     # 行動偏り閾値 (|left_ratio - right_ratio| > act_bias_threshold → 崩壊)

DQNAgent.baseline.nn_init_mode=1          # 0=default、1=XavierUniform、2=HeNormal
DQNAgent.baseline.alpha=5e-4              # 学習率 1e-3 3e-3 1e-4 1e-4 3e-4 5e-4
#DQNAgent.baseline.alpha=1e-3             # 学習率 1e-3 3e-3 1e-4 1e-4 3e-4 5e-4
DQNAgent.baseline.gamma=0.99              # ★ 0.99 0.995  γが高いほど「長期安定」を目指す
DQNAgent.baseline.eps_max=1.00            # 指数減衰  
DQNAgent.baseline.eps_min=0.05            # 0.1f 0.05f
DQNAgent.baseline.eps_decay_step=20000    # 20K
#DQNAgent.baseline.eps_sigmoid_step=20000 # 20K
DQNAgent.baseline.softupdate_tau=0.001    # ★ 1.0f 0.004f  0.01f 0.005f;
#DQNAgent.baseline.hardupdate_interval=5000 #  -1 5000; //200 500 1000
DQNAgent.baseline.use_grad_clip=true      #
DQNAgent.baseline.grad_clip_tau=25        # 10 25 40 1f 5f 10f
DQNAgent.baseline.use_td_clip=true        #
DQNAgent.baseline.td_clip_value=3.0       # 4.0
DQNAgent.baseline.eps_zero_step=-1        # 120000;
DQNAgent.baseline.use_double_dqn=true     # Double DQN 有効化フラグ（trueで有効）
DQNAgent.baseline.use_replay_buffer=true  #
DQNAgent.baseline.replay_capacity=10000   # ★
DQNAgent.baseline.replay_batch_size=128   # ★
DQNAgent.baseline.replay_warmup_steps=1000   #
DQNAgent.baseline.replay_update_interval=5   #  ★
DQNAgent.baseline.q_z_threshold = 2.6           # 2.0 1.5-2.0 3.0 z-score 崩壊判定閾値
DQNAgent.baseline.q_cv_threshold = 0.35         # 0.45 0.3 0.25-0.35 0.5 CV 崩壊判定閾値
DQNAgent.baseline.q_niqr_threshold = 0.8        # 0.65 0.5 0.4-0.5 0.6 NIQR 崩壊判定閾値
DQNAgent.baseline.act_bias_threshold = 0.35     # 行動偏り閾値 (|left_ratio - right_ratio| > act_bias_threshold → 崩壊)
DQNAgent.baseline.use_as_dqn = false            # Adaptive Stabilized DQN (AS-DQN)



# ■per_alpha
#  α（0≦α≦1）はExperienceサンプリングのランダム度合い。
#  α=0でランダムサンプリングと同一、α=1.0でGreedyに優先度を反映させる。
# ■per_beta
#  優先度に応じてサンプリングを行うと同じExperienceを何度も学習して学習安定性を損なう恐れがあるため、
#  TD誤差に対してサンプリング確率に応じた補正を行う。（サンプリング優先度が高いほどQ値への反映度合いを抑える）
#  学習初期はTD誤差が安定していないため、per_betaを小さくして上記補正を抑制する。
# 　per_beta=0　→ 補正無し、 per_beta=1.0 →完全補正
