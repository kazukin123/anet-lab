# DefaultDQNAgent / Atari 探索記録

Atari（ALE 直結）の DefaultDQNAgent 系 Run に関する探索索引です。

## campaign

| 期間 | 文書 | 主題 | 状態 |
|---|---|---|---|
| 2026-08-14〜2026-08-27 | [ベースライン定義（Breakout / Pong）](2026-08-17_baseline.md) | プロトコルプロファイル、num_envs、NN 構成、`grad_clip_tau`、PER β、spatial exploration、`replay_ratio` 探索、予算 | 主要軸は決着。数値は**旧バンドル**のもの（2026-08-27 に config を BTR 寄せへ変更した） |
| 2026-08-27〜 | [Atari-5 横断スイープ](2026-08-27_atari5.md) | Breakout で決めた構成の env 汎化、57 ゲーム median の推定。**新バンドル**（未探索ハイパラを BTR 初期値へ。**実行時は環境と `adam_eps` が寄せに失敗**。2026-08-29 に修正済み） | active。初回 20M で Atari-5 median 推定 **55.4% HNS**。3 ゲームが上昇中で予算不足 |
| 2026-08-28〜08-29 | [可塑性メトリクスの較正と保護機構](2026-08-28_plasticity.md) | PRD062 / 063 のメトリクスが損傷を検出するかの検証、コスト特性、正規化 × weight_decay の初回評価。**旧バンドル固定** | 器の較正は決着。保護機構は継続中。摩耗は勾配 step の関数で `replay_ratio` に依存しないことを確認 |

## 現時点の判断

最終更新: 2026-08-27

| 設定・探索 | 現在の扱い | 根拠の概要 |
|---|---|---|
| **`replay_ratio = 1`** | **採用。内点最適** | 8 / 4 / 2 / 1 / 0.5 の 5 点で山。100M 単変数比較の 60-100M プールで RR2 418.2 — **RR1 474.7** — RR0.5 402.5。exp 軸・勾配 step 軸・壁時計の三軸すべてで RR1 が最良で、トレードオフが存在しない。両側で律速が違う（高 RR = 可塑性の損傷、低 RR = 勾配 step 不足） |
| NN = IMPALA-CNN（Scale=2） | 採用 | Nature CNN 比で 50M 時 eval1 147.7 対 115.7。Nature は 44M でピークアウトするが IMPALA はピークが最後尾。≥400 点は IMPALA だけが出す。Scale=2 は BTR 準拠 |
| ViT ハイブリッド | 不採用 | `grad_clip_tau=30` で揃えると効果量が 1/3 に縮む。train median は +57% だが p75 / p90 / 1 画面超え率は IMPALA が上。**下振れを潰す装置であって上限を上げる装置ではない** |
| **spatial exploration（ε ラダー 上端 0.4）** | **採用** | ON/OFF と上端 4 点（0.01 / 0.2 / 0.4 / 1.0）で単峰。eval1 平均 257.7 → 358.3、`<100` 15.5% → 1.4%、p10 83 → 273。上側はほぼ動かず**下振れだけが消える**。0.4 対 0.2 は平均差 5.8σ。throughput コストはほぼゼロ |
| `grad_clip_tau` | **10**（BTR 値）。旧 30 | 5 は毎 update 発動して安全弁でなく正規化器になり、median 134 対 323 で劣る。30 は当時の `grad_norm` 6.0-15.4 に対する値で、RR1 の現在は平均 0.5-1.4（全区間 max 28.6）なので 10 でも平常時は不発。ブロック 12 の結論と矛盾しない |
| `weight_decay = 0` | 採用 | 0.1 は 0-30M で +40〜99% 先行するが 30M で交差し 45-50M で **−43.4%**。1 画面超えは全期間 0.09%。機構自体は働くが（`q_gap/q_max` −25%）成績は落ちるため、**Q の過鋭化は症状であって直接下げにいく対象ではない** |
| `per_beta_step` = 予算の半分 | 採用 | 10M < 50M。予算 100M に対しても 50M が最良で、「総予算に合わせる」という当初の運用則は実測が反証した |
| `num_envs = 128` | 採用 | throughput が 128 で頭打ち（+9.1%）、学習効率は 128 まで無傷で 256 から崩れる（−17%）。ただし**この結論は learner コストに依存**し、`replay_ratio` を 4 → 1 に変えた後の再評価は未実施 |
| BF16 | 採用。**ULP 余裕は監視不要** | γ 0.997 で相対 action gap が 4〜5 分の 1 に潰れ、余裕は 1.2-1.7 ULP。1 を割る局面でも成績は最良で、6 例の反例により**成績の予測子ではない**と確定した |
| `q_gap`（定常値） | **成績の予測子ではない** | 95-100M で RR2 0.0798 > RR1 0.0729 > RR4 0.0579 となりスコア順（RR1 > RR2 > RR4）と一致しない。可塑性の代理として使えるのは**過渡の深さ**だけであってレベルではない |
| プロトコル | **v5 + noop 30**（BTR 準拠。2026-08-29 から） | sticky 0.25 が Breakout で支配的（0 にすると train 生スコア 42 → 119）。`episodic_life` は単独では効果ゼロ。**2026-08-28 以前の全 Run は noop 0** — 2026-08-27 に root ラダーを 30 へ変更したものの `run.@v5_iqn_impala_x2` が `@v5_noop0` を再指定して打ち消しており、2026-08-29 に修正した |
| eval | eval1 = target net、ε 0.01、**1 本評価** | ε=0 は `fire_reset=false` と組で FIRE デッドロックに落ちる（`q_gap` が大きい良い方策ほど発生率が高い）。eval1 は全 Run で eval2（online net）よりスコアも安定性も上。**1 本評価は分散が大きく NN 構成の A/B を判定できない**（`060_eval_batch_episodes_10prd.md` 待ち） |
| `per_alpha` / `adam_eps` / `update_warmup_steps` / IQN τ 数 | **未探索。BTR 初期値を採用** | 0.2 / **1.95e-5**（2026-08-29 から） / 200,000 / 8（online・target・行動選択とも）。**2026-08-28 以前の全 Run は `adam_eps` = 1e-4** — `A1` に置いた値をチェーン後段の `@bf16` が上書きしており、A2 へ移して修正した。相関で最適でない組み合わせが出る可能性は許容し、今後の探索へ委ねる |
| `soft_update_tau = 0.001` | 据え置き（BTR は hard C=500 grad step） | BTR の C は RR4 前提で「32K env step ごと」。RR1 でそのまま写すと 128K exp step = **4 倍の遅れ**になる。soft → hard は値でなく機構の変更でもある（`940_target_update_step_axis_10prd.md`） |
| 予算 | 100M で相転移が起きる | ラダー構成では 45M→80M で `≥432` が約 3-4 倍に立ち上がる。ただし**ラダー無しの対照は 30M で完全に頭打ち**。予算が効くかどうかは予算の問題ではなく**データ被覆の問題**だった可能性がある |

### 到達水準（Breakout、旧バンドル）

`replay_ratio` 1 / 100M step（= 400M frames）/ **4.16h** で、eval1 90-100M は
**mean 487.1 / median 428 / p25 420 / `≥432` 31.2% / `<300` 0.0% / 単発最高 864**。
864 は **2 ラック（432 × 2）の完全掃き切り**で、800 超が 6 本出ている。HNS57 換算 1685%。

BTR の Breakout 682 に対し 71%、公開 CSV の最終値 602.01 に対し 81%。
ただし BTR は 100 本平均・online net・最良チェックポイント報告であり、
本記録は 1 本・target net・window 平均なので**同列ではない**
（`reports/btr_hyperparams_survey_2026-08-26.md` §7・§11）。

## Atari での評価観点

### ラック境界を基準に読む

Breakout の 1 画面は 6 段 × 18 個で **432 点**、2 画面で 864 点。
平均スコアだけでなく `≥432` の割合を見る。平均が +6% しか動かない区間で `≥432` が 4 倍になることがあり、
**予算延長は上側の裾だけを買う**ことがはっきり出る。

低スコア帯の実体は「停止」ではなく「短命」である（`score<50` かつ `game_len>3000` は 0 本、
train 側に FIRE デッドロックは 1 本も無い）。「上手いエピソードは伸びるがポカる率は下がらない」という
分離が起きるため、**上側（`≥432`）と下側（`<100`）は別々に読む**。

### 集約の分母に注意

`game_score` / `game_len` / `game_frames` は実 game over / truncation でのみ非 NaN になり、
バッチ集約は NaN を分母から除外する。したがって `mean.` の分母は num_envs ではなく
**その step で実際にゲームを終えた env の数**である。同時完了率は λ = num_envs / 平均ゲーム長 で決まり、
Breakout（128 env / 約 1,900 step）では実測 97% が単独完了なので、`mean.` は事実上
「ゲーム 1 回の素点の列」になる。**ゲーム長が短い題材ほど平均化が効いてピークを潰す**ため、
ゲーム横断の比較では `max.` を併置して読む（`42_env/16_game_score_max` 等）。
`lives` だけは常時確定なので分母は num_envs であり、これは本物のバッチ平均。

### eval 点の密度は `replay_ratio` に依存する

eval interval は learn_step 基準なので、**RR を下げるほど exp 軸上の eval 点が減る**。
100M / interval 2500 の実測で 10M 窓あたり RR4 63.1 点 / RR2 31.3 / RR1 15.7 / RR0.5 7.9。
窓別の比較はこの粒度に律速されるため、**RR をまたぐ比較では点数差を必ず確認する**。

### 判定はブレ幅基準

成績差は単一 Run の last 値ではなく、同等設定の複数 Run / seed 違いの終盤平均ブレ幅で判断する。
ブレ幅内は有意差なし、超えてはじめて構造差として扱う。
実効設定の確認は live config ではなく `runs/<name>/config/config_data.txt`（実行時ダンプ）を正本とする。

### 過渡は深さで読む

多くのメトリクスが序盤にピーク → 急落 → 緩回復する過渡が観測されるが、
**位置は grad 軸にも exp 軸にも揃わず、深さだけが `replay_ratio` に単調**である
（RR8 −62% / RR4 −31.5% / RR2 −11.8% / RR1 なし / RR0.5 なし）。
`q_gap` の過渡の深さは可塑性の代理として使えるが、**定常のレベルは使えない**（前掲）。

表現側を `34_agent_plasticity` 群で直接測った結果、**摩耗そのものは勾配 step の関数で `replay_ratio` に
ほぼ依存しない**ことが分かった（同 grad step で `weight_norm_feature` 63.9 対 67.4、`dead_ratio` の
谷からの倍率 5.9 対 5.8）。RR が決めるのは摩耗量ではなく、**その摩耗を買うのに何件の新規データが
付いてきたか**である。したがって価値側（`q_gap`）が RR の関数、表現側が勾配 step の関数、という
役割分担になる（[2026-08-28_plasticity.md](2026-08-28_plasticity.md)）。

主読みは `dead_ratio`（谷が転換点を予告）と `weight_norm_feature`（絶対値が勾配軸で揃う）の 2 本。
**srank は今回の損傷モード（ユニットの死）には反応しない** — δ 3 種・probe batch 2 種で深さ −3〜6%、
同じ事象で `dead_ratio` は +490%。読み方は `docs/design/030_user_guide_analysis.jp.md` 4.7 節。

## 探索環境としての位置付け

Atari は文献比較の土俵が明確な唯一の env であり、BTR（arXiv:2411.03820）/ Nature DQN 2015 /
Dopamine 系のどれと比べるかでプロトコルプロファイルを選ぶ。**スコアはプロファイル間で直接比較できない**ため、
比較先がどの wrapper 構成かを毎回確認する（`docs/design/220_atari_env.jp.md` 3 章）。

**Breakout 単独での判断には固有の危険がある。**

- `reward_clip=true` により learner にはどのブロックも +1 だが、`game_score` は 1 / 4 / 7 で数える
- 432 / 864 のラック境界が作る相転移は他ゲームに無い構造である
- ε ラダーが下振れを潰す効果が被覆由来なら env 非依存のはずだが、1 ゲームでは検証できない

このため 57 ゲームの median を推定するサブセット **Atari-5**（battle_zone / double_dunk /
name_this_game / phoenix / qbert）を横断確認に使う。設定は `run.@a5*`（`apps/runner/config/Atari.txt`）と
`apps/12_batch_run_atari5.bat` にある。

初回スイープ（20M）の実測で、この懸念は裏付けられた。5 ゲームの hns57 は **−77.3%（double_dunk）〜
174.1%（name_this_game）** に散らばり、正規化バンド幅も 2.2 〜 34,828 と 4 桁違う。
**生スコアの横断比較は成立しない**ので、集約は必ず hns57 と Atari-5 回帰で行う
（[2026-08-27_atari5.md](2026-08-27_atari5.md)）。

詳細な条件、当時の解釈、判断の更新履歴は campaign 文書を参照してください。
