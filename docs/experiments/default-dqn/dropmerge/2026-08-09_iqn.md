# DropMerge IQN 導入・QR 比較の探索履歴

## メタデータ

| 項目 | 内容 |
|---|---|
| 対象 Agent | DefaultDQNAgent |
| 対象 Env | DropMerge |
| 開始日 | 2026-08-09 |
| 最終更新 | 2026-08-13 |
| 状態 | active。tau sampling比較はstratified採用で完了。次はtarget tau本数Mの探索 |
| 主目的 | IQN が DropMerge で成立するか確認し、既存 QR 長期ラインに対する sample efficiency・Q 値安定性・NEET 挙動を評価する |
| 比較上の注意 | 非決定論backend。sampling finalistは2 seed、50Mはseed 2比較。Run名ではなくartifactの実効設定を正本とする |

## 現時点のまとめ

1. 同一のB256/RR1、`alpha=1e-4`、PER alpha 0.2、initial priority fixed 0.3、`use_optimistic_target=false`で、IQN32 randomとQR51の100M scratch比較が成立した。実効設定差はquantile modeとそれに伴うhead / tau経路が中心である。
2. 90–100MのQR51はEval target reward EMA約1629、Policy reward EMA約1584で、IQNの約1508 / 1398をそれぞれ約8.1% / 13.4%上回った。QRは序盤から優位で、IQNが20–40Mに一度接近した後、40M以降に再び差が開いた。
3. QR51の90–100M target rewardは過去B128/RR1.25 QRの約1628と同水準であり、B256/RR1は100M品質を保ちながら終盤throughputを約3822 exp/sまで上げた。IQNは約3518 exp/sで、QRが約8.6%高速だった。
4. QR / IQNともQ、TD、loss、gradient、PERは安定し正常closeした。差は単純な発散ではなく、現在のDropMergeではrandom tau IQNのsample efficiencyがQRより低いという観測である。ただしnon-deterministic backendの各1 Runであり、因果確定や最終汎化性能の優劣とはしない。
5. QR51のPRD048診断ではquantile crossing約36%、lower-risk/full-Q不一致約60%、upper-tail/PER priority Spearman約0.62だった。期待値成績は有効だが、tailを整列した分位関数として読む信頼性は低く、今後のstratified IQNでもcrossingを先に確認する。
6. 20Mのtau sampling比較ではstratified / antitheticがrandomを上回り、systematicはrandom同等、fixedは10M以降に停滞した。seed 2再現でも両finalistの改善は維持され、tau sampling改善は単発上振れだけでは説明しにくい。
7. 2-seedの0–20M平均はstratifiedがTarget 853 / Policy 797、antitheticが841 / 800で実質同率だった。seed 2の40–50Mもantithetic 1296 / 1205、stratified 1271 / 1199だが、対応step差の95%区間は0をまたいだ。
8. sampling方式は、2-seed Target、全stratum被覆、rare transitionでtau範囲を外しにくい目的との一致からstratifiedを採用する。antitheticは有力な代替候補として保持し、次はK=N=M=32のstratified基準からtarget本数Mを探索する。

### 2026-08-09時点のB128/RR1.25 IQN基準で得た知見

1. IQN 32/32 random は DropMerge で正常に学習し、`100,001,792 exp-step`でAutoPauseした。NaN、持続的 Q 発散、grad clipping 張り付きはなく、終了後に`agent_close.anet`も正常保存された。
2. 既存 QR51 baseline より reward と max rank の立ち上がりは遅い。90–100M の Eval target reward EMA 平均は IQN 約 1366、QR 約 1628で、差は約16.1%。IQNのDouble Suikaは未観測である。
3. IQN では 30–40M に Q 値全体の一時的な膨張が生じ、約 39.75M で `q_max_real_max=30.61` に達した。50M 付近までに QR と同程度の Q 水準へ戻り、その後は安定している。
4. Q 膨張期には Eval の NOOP 優位率も上昇し、36–38M で Eval1 約 48%、Eval2 約 31% に達した。NOOP 優位が消えるのと並行して Q 値も正常化したため、一時的な NEET/Q 自己強化ループの鎮火が有力仮説である。
5. ReplayBuffer 満杯は約 0.524M、warmup 経験の完全 overwrite は約 0.550M であり、その境界に Q、TD error、reward の急変はない。30–50M の Q 遷移を LunarLander と同じ「満杯境界の相転移」とは扱わない。
6. LunarLander と共通するのは、IQN が経験分布や方策分布の変化に対して QR より大きな Q・loss・grad 応答を示す可能性である。引き金は Env ごとに異なり得る。
7. 現 Run は歴史的 QR51 baseline に主要設定を合わせた実用比較であり、厳密な QR/IQN A/B ではない。QR51 と IQN32/32、Eval tail score の意味差が残る。
8. 80–90Mから90–100MへEval target rewardは約2.3%改善し、QRとの差も72M時点の約19%から約16%へ縮小した。ただしDouble Suika 0、single-seed、非決定論であり、200M延長を正当化する強い改善証拠まではない。
9. 次の優先診断は、IQN の current / target / train-policy tau を `fixed` にした deterministic fixed-grid control である。train policy だけの fixed 化と、learner まで含めた fixed-grid control を混同しない。

## 参照 Run

| Run | mode | 主な用途 | 到達状況 |
|---|---|---|---|
| `run_20260809-033908_dm_iqn32-b128-rr125` | IQN 32/32 random | 現在の IQN 基準。実効 RR 1.25に合わせて終了後にディレクトリ名を訂正 | 100,001,792、正常AutoPause・正常close |
| `run_20260727-021415_apx-ll_cy01` | QR51 | 初期長期 Run の比較基準 | 約 200M、正常 close |
| `run_20260811-113930_dm_iqn32-b256-rr100★` | IQN 32/32 random | B256/RR1高速基準。同条件QR51との直接比較 | 約100M、正常AutoPause・正常close |
| `run_20260812-003400_dm_qr51-b256-rr100` | QR51 | B256/RR1高速基準。同条件IQN32との直接比較 | 約100M、正常AutoPause・正常close |
| `run_20260812-104705_dm_iqn32-b256-rr100` | IQN 32/32 random | non-deterministic backendでのrandom反復対照 | 約100M、正常AutoPause・正常close |
| `run_20260813-001049_dm_iqn32-stratified-b256-rr100` | IQN 32/32 stratified | 各stratumを独立に被覆する候補 | 約20M、正常exit・正常close |
| `run_20260813-014857_dm_iqn32-antithetic-b256-rr100` | IQN 32/32 antithetic | 中点対称ペアによる候補 | 約20M、正常exit・正常close |
| `run_20260813-032409_dm_iqn32-systematic-b256-rr100` | IQN 32/32 systematic | 等間隔・行単位random phaseのcontrol | 約20M、正常exit・正常close |
| `run_20260813-045721_dm_iqn32-fixed-b256-rr100` | IQN 32/32 fixed | 決定論的midpoint gridのcontrol | 約20M、正常exit・正常close |
| `run_20260813-111617_dm_iqn32-stratified-b256-rr100` | IQN 32/32 stratified、seed 2 | stratified再現と50M基準 | 約50M、正常AutoPause・正常close |
| `run_20260813-163349_dm_iqn32-antithetic-b256-rr100` | IQN 32/32 antithetic、seed 2 | antithetic再現と50M比較 | 約50M、正常AutoPause・正常close |

IQN Run の初期設定確認用 Run は正式比較から除外する。各探索ブロックでは、上表のうち比較契約に合うartifactを明記して使用する。

## 採用したIQN探索基準の実効設定

`run_20260813-111617_dm_iqn32-stratified-b256-rr100/config/config_data.txt`を正本として確認した。

| 分類 | 設定 |
|---|---|
| quantile | `quantile_mode=iqn`, learner current N=32 stratified、target M=32 stratified |
| policy | train UQE tail mean、K=32 stratified、spatial exploration ON。Eval UQE tail mean、K=32 fixed |
| target | `use_optimistic_target=false`、Greedy、full-range fixed |
| learner | B256、RR1、`alpha=1e-4`、`gamma=0.997`、n-step 1、soft update tau 0.005 |
| Replay / PER | capacity 524,288、warmup 25,600、PER alpha 0.2、beta 0.4→0.6 / 20M、initial priority fixed 0.3 |
| backend | BF16 learner、TF32 ON、non-deterministic、seed 2、256 env |
| Env | `direct_noop`、NoDrop timeout 100 / penalty -10、NoLegal Phase 2 ON、prev-action trio / DROP marker ON |

後続探索ではseed 2の50M stratified artifactを比較基準とし、Run名ではなく`config/config_data.txt`のK/N/Mとsample modeを毎回確認する。

## 探索ブロック 01: IQN 32/32 の初期成立性と QR51 比較

**記録時点:** 2026-08-09、約 72M 時点
**状態:** completed / 正常成立、QR51のsample efficiencyには未達
**確度:** medium-low、single-seed、比較契約に差あり

### 探索観点

LunarLander で成立確認した IQN を DropMerge の既存 QR 長期ラインへ移し、学習成立、sample efficiency、Q 分布、PER、NEET、throughput を確認する。

### 探索結果

60–72M の同一 exp-step window では、IQN は QR51 baseline より遅かった。

| 指標 | IQN | QR51 | 差の概略 |
|---|---:|---:|---:|
| Eval target reward EMA 平均 | 約 1294 | 約 1605 | IQN 約 -19% |
| Eval policy reward EMA 平均 | 約 1220 | 約 1542 | IQN 約 -21%。policy 意味差あり |
| Eval1 max-rank EMA | 約 10.13 | 約 10.53 | IQN が低い |
| Eval2 max-rank EMA | 約 10.04 | 約 10.46 | IQN が低い |
| Double Suika | 両 Eval 0 | QR Eval2 で少数 | IQN は未到達 |
| throughput | 約 2303 exp/s | 約 2525 exp/s | IQN 約 -9% |

同じ wall-clock では QR がより多い exp-step へ到達するため、実時間差を含む性能差はさらに大きい。

### 成立性

- stderr は空で、NaN、OOM、持続的 loss / TD / Q 発散はない。
- 60–73M の `q_std` は IQN 約 0.039、QR 約 0.038 で、分布 collapse はない。
- PER unsampled eviction は両者とも約 20%で、IQN 固有の Replay coverage 破綻は見えない。
- Q 値の過渡変動はあるが、50M 以降は TD error、loss、grad norm とともに安定した。

### 比較上の制約

- QR baseline は QR51、IQN は N/M=32/32 であり、quantile 数と head parameterization が一致しない。
- IQN Eval2 は tail mean score を使う。古い QR artifact の Eval policy と意味が完全一致しない。
- 同じ seed でも NN 構造と乱数消費列が違うため、軌道一致は期待しない。

この Run から言えるのは「現在の IQN32/32 構成は歴史的 QR51 baseline より遅い」までであり、IQN 一般が QR より劣るとは結論しない。

## 探索ブロック 02: ReplayBuffer 満杯境界と Q 値遷移

**記録時点:** 2026-08-09
**状態:** completed / 満杯境界主因説を棄却
**確度:** medium

### 探索観点

LunarLander では ReplayBuffer が grow-only から overwrite へ切り替わる境界で Q、loss、grad、reward が同時に変化した。DropMerge の 30–50M Q 遷移も同じ機構かを確認する。

### 境界計算

```text
ReplayBuffer 満杯              = 524,288 exp-step
warmup 経験の overwrite 開始  = 524,288 exp-step
warmup 経験の完全 overwrite    = 524,288 + 25,600
                              = 549,888 exp-step
```

256 env 換算では、warmup 終了が train step 100、満杯が 2,048、warmup 経験の完全忘却が 2,148 である。

### 満杯直前・直後

| 区間 | `q_max_real_max` | TD mean EMA | Eval target reward EMA |
|---|---:|---:|---:|
| 0.450–0.525M | 1.93 | 0.02425 | 563.9 |
| 0.525–0.600M | 2.26 | 0.02409 | 564.5 |

Q は滑らかに増えており、TD error と reward に急変はない。QR baseline も同様だった。

### 判断

30–50M の Q 遷移は、満杯境界から約 40M 遅れている。Replay 内の経験分布は方策成長に伴って満杯後も変わり続けるため間接影響は否定しないが、LunarLander と同じ満杯時点の相転移ではない。

## 探索ブロック 03: Q 値バブルと一時的 NEET

**記録時点:** 2026-08-09
**状態:** active hypothesis
**確度:** medium、強い時系列相関はあるが介入実験なし

### Q 値の推移

| exp-step | `q_sa_real_mean` | `q_max_real_max` | TD mean EMA | Eval target reward EMA |
|---|---:|---:|---:|---:|
| 10–20M | 8.50 | 15.57 | 0.0301 | 890 |
| 20–30M | 9.35 | 18.54 | 0.0364 | 1076 |
| 30–40M | **14.15** | **28.50** | **0.0412** | 1138 |
| 40–50M | 11.70 | 24.09 | 0.0405 | 1214 |
| 50–60M | 8.19 | 13.00 | 0.0315 | 1266 |
| 60–73M | 8.27 | 12.98 | 0.0311 | 1294 |

約 39.75M で `q_max_max=4.93`、`q_max_real_max=30.61` に達した。単発スパイクではなく、`q_sa_real_mean` も上昇したため、価値分布全体の広い Q 水準変化である。

Q が約半分へ下がる一方で Eval reward は上昇した。高い Q が性能向上を表したのではなく、一時的に膨らんだ価値尺度を実リターンへ再較正したと解釈する。

### NOOP 優位との同期

NOOP EMA 系 metric は `$exp_step`を指定しておらず、artifact 上の step 軸は train step である。以下は `num_envs=256` を掛けて exp-step へ換算した。

| exp-step | Eval1 NOOP 優位率 EMA | Eval2 NOOP 優位率 EMA | `q_sa_real_mean` |
|---|---:|---:|---:|
| 30–32M | 2.8% | 3.3% | 13.3 |
| 34–36M | 21.8% | 17.2% | 14.5 |
| 36–38M | **48.0%** | **30.8%** | 14.0 |
| 40–42M | 14.6% | 0.2% | 13.9 |
| 44–46M | 10.6% | 1.1% | 12.0 |
| 46–48M | 0.3% | 0.4% | 10.4 |
| 50–52M | 0.1% | 2.6% | 8.1 |

NOOP margin の区間平均は負を維持したため、全状態で強い NOOP 優位になったわけではない。NOOP と DROP が近い状態で順位が広く揺れたと考える。

Eval episode step EMA は 32–34M の約 563 / 496 から、52–54M の約 250 / 243 へ短縮した。max rank と fruit count が維持され、reward は上昇しているため、単なる早期 GameOver より待機行動の減少と整合する。

NoDrop timeout 終端は NOOP 優位期に張り付かなかった。100 step 連続 NOOP する単純 NEET ではなく、タイムアウト前に DROP を混ぜる一時的な待機方策だった可能性がある。

### 機構仮説

`gamma=0.997`、n-step 1 では、NOOP に近い自己遷移の 1-step Bellman 差は小さい。Q(NOOP) が近傍 DROP をわずかに上回ると、NOOP 経験が Replay へ増え、bootstrap により近い価値を自己維持しやすい。

一方、100 step 待つ割引は `0.997^100 ≈ 0.74` であり、本来は長期的に不利である。長い待機軌跡と後続の失敗が Replay に十分蓄積されると、n-step 1 で遅れて不利が伝播し、NOOP 順位と Q 水準がともに訂正されたと考える。

この仮説では、IQN が NEET を新設したのではなく、既存の NOOP 近傍不安定性を大きな Q 値バブルへ増幅したことになる。

## 探索ブロック 04: QR から IQN への変更が Q 応答を増幅する理由

**記録時点:** 2026-08-09
**状態:** active hypothesis
**確度:** medium-low、LunarLander と DropMerge の cross-Env 傾向あり

### 観測事実

- 過去の QR でも一時的 NEET スランプはあったが、Q 全体の明瞭な山として記憶・記録されていない。
- DropMerge IQN では NOOP 順位だけでなく learner の平均 Q 水準も上昇した。
- LunarLander でも IQN 化後に Q、loss、grad の変化が QR より大きく見える傾向があった。

### IQN 固有の増幅候補

1. **train-policy の tail tau sampling**
   QR は固定 quantile 出力の上側を評価する。現在の IQN train policy は actor ごとの上側 tau 範囲から 32 点を毎 forward ランダム生成する。NOOP と DROP が近い状態では、train 時の順位が QR より揺れやすい。
2. **learner target の Monte Carlo 分散**
   IQN learner は current N=32 と target M=32 を毎 update 独立にランダム生成する。target action は deterministic Greedy だが、選択 action の target value には M=32 の Monte Carlo 分散が残る。
3. **tau 条件付き共有表現**
   IQN は `main_feature * tau_embedding` を head へ流し、quantile 間で表現を共有する。共有 value 表現がずれると、多数の tau と全 action の基準値が同方向へ動きやすい。
4. **Replay feedback**
   tail score の小さな順位誤差が NOOP 経験分布を変え、その変化が learner の全域平均 Q まで押し上げる。引き金と平均 Q バブルは別段階と考える。

learner の `37_agent_qtd/*` は全域 tau を使った平均 Q である。したがって 30–40M の広い Q 山は、Eval UQE tail score の表示だけでは説明できない。random 32 点による瞬間ノイズも、10M 規模の平均変化に比べて小さい。

### LunarLander との統合解釈

- LunarLander の引き金: ReplayBuffer 満杯・初期経験 overwrite に伴う経験分布切替が有力。
- DropMerge の引き金: NOOP 近傍の方策自己強化が有力。
- 共通の増幅器: IQN の stochastic tau と共有 quantile function が、経験分布変化に対する Q 応答を大きくする可能性。

cross-Env の方向は一致するが、まだ各 single-seed であり一般則とはしない。

## 探索ブロック 05: IQN fixed-grid control

**記録時点:** 2026-08-13、20M close後
**状態:** completed / fixedは10M以降に停滞。詳細は探索ブロック07
**確度:** medium、同一seedだがnon-deterministic backendの単一Run

### 探索目的

長期の IQN ハイパラ探索へ入る前に、tau sampling を決定論的な`fixed`へ寄せた IQN が QR に近い傾向を再現するか確認する。

`train_policy.tau_rule.sample_mode=fixed` だけを変える診断は、train action score の Monte Carlo 揺れだけを外す。learner current / target が random のままなので、QR 近似 IQN とは区別する。

### config-only で作れる deterministic-grid IQN

最小の 32/32 control:

```ini
DefaultDQNAgent.learner.iqn.current_taus.num_taus = 32
DefaultDQNAgent.learner.iqn.current_taus.sample_mode = fixed
DefaultDQNAgent.learner.iqn.target_taus.num_taus = 32
DefaultDQNAgent.learner.iqn.target_taus.sample_mode = fixed
DefaultDQNAgent.train_policy.tau_rule.num_taus = 32
DefaultDQNAgent.train_policy.tau_rule.sample_mode = fixed
```

Eval と target policy は現在も fixed である。比較時は artifact で再確認する。

### QR 等価性の限界

fixed にすると learner の積分点と target が等間隔の中心へ固定され、QR の固定分位グリッドへ近づく。しかし次は残る。

- QR は固定 quantile 出力、IQN は tau embedding を通す共有関数であり、parameterization と gradient 経路が違う。
- IQN UQE tail mean は `[tau,1]`へ直接固定点を置く。QR は全域固定 quantile のうち tau 以上を抽出するため、action query の点数と位置が一致しない。
- 厳密な UQE action semantics の一致には、IQN でも全域固定 grid を query して QR と同じ index 規則で tail を抽出する実装 mode が必要である。

したがって名称は「QR 等価 IQN」ではなく `IQN fixed-grid control` とする。

### 比較案

| 目的 | 比較 | 長所 | 注意 |
|---|---|---|---|
| 現在 IQN の Q 山原因を切る | IQN32 random vs IQN32 fixed | tau sampling の寄与を一軸で確認 | QR との直接比較ではない |
| formal な実装差を切る | QR32 vs IQN32 fixed | quantile 数と pair 数を一致 | QR32 Run を新規に要する |
| 歴史的 QR51へ寄せる | QR51 vs IQN51 fixed | cy01とのquantile数を一致 | IQN51 は計算・メモリ負荷が大きい |

最初は IQN32 fixed を 20M gate で実行し、Eval reward、Q slope、TD、NOOP 優位率を確認する。QRとの差が縮む、または current random IQN より安定する場合だけ 50Mへ延長する。formal A/Bが必要なら QR32を追加する。

## 100M 時点の判断更新

90–100M の確定 window。同一exp-stepのQR51 baselineと比較した。

| 指標 | IQN | QR51 baseline | 判断 |
|---|---:|---:|---|
| Eval target reward EMA 平均 | 約 1366 | 約 1628 | IQN 約 -16.1% |
| Eval policy reward EMA 平均 | 約 1311 | 約 1557 | IQN 約 -15.8%。policy意味差あり |
| Eval1 max-rank EMA | 約 10.20 | 約 10.53 | IQN が低い |
| Eval2 max-rank EMA | 約 10.14 | 約 10.46 | IQN が低い |
| Eval episode step EMA | 約 256 / 249 | 約 285 / 279 | IQNのepisodeが短い |
| `q_sa_real_mean` | 約 8.53 | 約 8.36 | 同水準 |
| `q_max_real_max` | 約 13.30 | 約 13.31 | 同水準 |
| TD mean EMA | 約 0.0308 | 約 0.0288 | IQNがやや高いが安定 |
| loss EMA | 約 0.0168 | 約 0.0244 | N差があるため大小を性能比較しない |
| PER unsampled eviction | 約 19.8% | 同程度 | IQN固有のcoverage破綻なし |
| Double Suika | Eval1 / Eval2 とも 0 / 977 | Eval1 / Eval2 とも 0 / 977 | 終盤windowでは両者未達 |

80–90Mから90–100Mへ、IQNのEval target rewardは約2.3%、policy rewardは約3.4%上昇した。完全な停滞・劣化ではなく、QRとの差も60–72Mの約19%から約16%へ弱く縮小している。ただしmax-rank差とDouble Suika未達が残り、現時点で長期最終性能の逆転を示す証拠はない。

Qは30–50MのバブルからQRと同程度へ戻った後、100Mまで再膨張しなかった。`q_std`、TD error、loss、grad normは安定し、gradient clippingもほぼ発生していない。

終盤のNOOP診断も鎮火を示す。最終付近のNOOP優位率EMAはEval1約4.08%、Eval2約2.26%、marginはそれぞれ約-0.028、約-0.047だった。reset NOOP marginもEval1約-0.020、Eval2約-0.020で負を維持し、持続的Eval NEET再発の証拠はない。

一方、90–100Mの`SpawnBlocked`終端はEval1約91%、Eval2約93%で、QR対照より高い。IQNはepisodeを短く終えるが高rank構成へ十分つながっていない可能性があり、性能面の黄色信号として残す。終端契約とpolicy意味差があるため、単一Runから原因は断定しない。

学習は03:39頃から16:45頃まで約13時間7分で、平均throughputは約2,118 exp/s。`100,001,792 exp-step`で正常AutoPauseし、16:55に852,107,726 bytesの`agent_close.anet`を保存した。stderrは空である。IQN32 randomは「正常成立し、後半も弱い改善は残るが、100Mのsample efficiencyではQR51未達」として区切る。

## 探索ブロック 06: B256/RR1同条件 QR51 / IQN32 100M比較

**記録時点:** 2026-08-12、100M close後
**状態:** completed / QR51を高速探索baselineとして採用
**確度:** medium、同条件A/Bだがnon-deterministic backendの各1 Run

### 探索目的と比較契約

DropMergeの1 Runを短縮しつつ評価品質を維持できる基本条件としてB256/RR1を固定し、QR51とIQN32 randomのsample efficiency、最終window、学習健全性、実時間性能を比較した。

実効設定の正本は次のartifactである。

- `apps/runner/workspaces/dm-iqn/runs/run_20260811-113930_dm_iqn32-b256-rr100★/config/config_data.txt`
- `apps/runner/workspaces/dm-iqn/runs/run_20260812-003400_dm_qr51-b256-rr100/config/config_data.txt`

両RunはB256、RR1、`alpha=1e-4`、`gamma=0.997`、n-step 1、PER alpha 0.2、initial priority fixed 0.3、BF16、256 env、`use_optimistic_target=false`を共有する。backendはTF32 / cuDNN benchmark有効、deterministic algorithms / cuDNN deterministic無効である。主な変更軸は`quantile_mode=iqn`と`quantile_mode=qr`、およびそれに伴うtau embedding / head経路である。

### 90–100Mの主要結果

| 指標 | QR51 B256/RR1 | IQN32 B256/RR1 | 判断 |
|---|---:|---:|---|
| Eval target reward EMA | 1629.4 | 1507.9 | QR +8.1% |
| Eval Policy reward EMA | 1584.3 | 1397.6 | QR +13.4% |
| Eval1 max-rank EMA | 10.572 | 10.398 | QR +0.174 |
| Eval2 max-rank EMA | 10.506 | 10.261 | QR +0.245 |
| Eval1 episode step EMA | 271.2 | 268.5 | ほぼ同等 |
| Eval2 episode step EMA | 268.6 | 257.0 | QR +4.5% |
| Train reward EMA | 1478.4 | 1267.0 | QR +16.7% |
| `q_sa_real_mean` | 7.13 | 9.05 | IQNはQが高いがscoreへ結びつかない |
| `q_max_real_max` | 12.15 | 15.02 | 同上。両者とも発散ではない |
| TD mean / std EMA | 0.0290 / 0.0450 | 0.0321 / 0.0474 | QRがわずかに低い |
| PER never-sampled eviction | 28.88% | 29.28% | ほぼ同じ。RR1由来のcoverageコスト |
| 終盤throughput平均 | 3822 exp/s | 3518 exp/s | QR +8.6% |

生rewardの中央値もTargetでQR 1768、IQN 1487、PolicyでQR 1684、IQN 1240だった。EMAの一時的peakだけではなく、終盤episode分布全体でQRが優位である。

QRのTarget / Policy reward EMAは60–80Mの1529 / 1454、80–90Mの1580 / 1554、90–100Mの1629 / 1584と継続上昇した。IQNは20–40MにTarget側で一度接近したが、40M以降はQRとの差が再び開いた。現在の条件ではrandom tau IQNの積分近似と共有tau表現がsample efficiencyの負担になっている可能性と整合する。

### 学習・PER・NOOP健全性

QRの60–100MではTD mean、TD std、loss、grad normが緩やかに低下し、gradient clippingはほぼ0だった。Qはrewardとともに緩やかに上昇し、IQNで観測した30–50Mの大きなQバブルは再現しなかった。QRとIQNでquantile lossの集約契約が異なるため、raw lossの大小は方式間の性能比較に使わない。

QRの90–100Mはpriority ESS ratio約0.310、IS ESS ratio約0.959、priority clip 0、initial sample / mass ratio約71.2%であり、PER collapseはない。never-sampled evictionはIQNと同程度の約29%で、方式差よりRR1のsample budgetを反映する。過去B128/RR1.25 QRの約19.6%より悪いため、PER探索時のcoverage課題として残す。

終盤のQR NOOP UQE marginはEval1約-0.024、Eval2約-0.025、NOOP win rateは実質0だった。IQN終盤の小さいNOOP優位率より低く、QRで持続的なNOOP attractorは観測しない。

### PRD048 分位tail診断

QR51の90–100Mで、PRD048の5metricは次の値だった。

| Metric | 値 | 解釈 |
|---|---:|---|
| `policy_upper_truncated_std` | 0.0354 | 選択行動のmedianより上側の幅 |
| `policy_lower_truncated_std` | 0.0341 | 選択行動のmedianより下側の幅 |
| tail asymmetry導出値 | 約0.019 | 上下幅はほぼ対称 |
| `lower_risk_full_q_argmax_disagreement` | 59.7% | 係数1のlower penaltyで期待値argmaxが大きく変わり得る |
| `quantile_crossing_ratio` | 36.3% | tau順tailを分位関数として読む信頼性は低い |
| `upper_tail_priority_spearman` | 0.623 | PER抽出済みminibatch内ではupper幅とraw priorityに中程度以上の順位相関 |

crossingが高いため、upper / lower幅を厳密な分位tailまたはuncertaintyと断定しない。期待値baselineの成績は有効だが、将来の探索bonus、risk penalty、stratified IQNのtail比較ではcrossingを先に妥当性gateとして確認する。SpearmanはPERで偏ったminibatch内の条件付き相関であり、ReplayBuffer全体での冗長性を示さない。

### Env成果と黄色信号

EvalのDouble Suikaは両方式とも未観測だった。QRではTrainにDouble Suika生成記録があり、90–100MのEval Rank11到達はEval1 232 / 391、Eval2 206 / 391だった。

一方、QRのSpawnBlocked終端はEval1約95.1%、Eval2約95.4%で、IQNの約88.6% / 90.2%および過去QRより高い。高いreward / max-rankを得た後にSpawnBlockedで終わる傾向であり、100Mの探索baseline採用を妨げないが、Double Suikaや長期終端品質が改善した証拠とはしない。

### 実時間・close状態と判断

QRは`99,999,488` learner exp-step付近まで到達し、約7.86時間でAutoPauseした。IQNは約8.33時間であり、同じ100M予算でQRが約28分短かった。QRは2026-08-12 10:08に899,156,351 bytesの`agent_close.anet`を正常保存し、IQNも852,107,808 bytesで正常close、両者の`stderr.log`は空だった。

結論は次のとおり。

- `QR51-B256-RR1`を、DropMerge分布RLハイパラ探索の高速baselineとして採用する。
- `IQN32-random-B256-RR1`をIQN側の比較baselineとして保持する。
- QRを200Mへ延長せず、次はtau sampling方式を一軸で比較する。
- 最初の候補は各区間を被覆しつつforwardごとのランダム性を残す`stratified`とする。20M kill gate、通過時50M、finalistのみ100Mへ進める。
- 同設定3 Runの確認はfinalist決定後に行い、今回の各1 Run差をseed / 非決定論分散から分離する。

## 探索ブロック 07: IQN tau sampling 5方式の20M screening

**記録時点:** 2026-08-13、全4 control close後

**状態:** completed / stratifiedとantitheticをfinalist採用

**確度:** medium-low。同一seed・同一設定の一軸比較だが、backendはnon-deterministicで各方式1 Run

### 探索目的と比較契約

IQN32 randomのsample efficiencyを改善するため、被覆軸`random -> stratified -> systematic -> fixed`と対称性軸`random -> antithetic`を20Mで比較した。B256、RR1、N/M/K=32、`alpha=1e-4`、PER alpha 0.2、initial priority fixed 0.3、`use_optimistic_target=false`、seed 1を維持し、train Policy、learner current、learner targetの`sample_mode`だけを方式ごとに揃えて変更した。Eval Policyとfull-distribution queryは全IQN Runで`fixed`を維持した。

実効設定と正常終了は各Runの`config/config_data.txt`、`agent_close.anet`、最終log、空の`stderr.log`で確認した。比較対象は次のartifactである。

- random: `run_20260811-113930_dm_iqn32-b256-rr100★`、`run_20260812-104705_dm_iqn32-b256-rr100`
- stratified: `run_20260813-001049_dm_iqn32-stratified-b256-rr100`
- antithetic: `run_20260813-014857_dm_iqn32-antithetic-b256-rr100`
- systematic: `run_20260813-032409_dm_iqn32-systematic-b256-rr100`
- fixed: `run_20260813-045721_dm_iqn32-fixed-b256-rr100`
- 参考QR: `run_20260812-003400_dm_qr51-b256-rr100`

### 主要成績

Eval raw値の区間平均を使う。20M RunはEval 781点、最終Eval step `19,993,856`まで到達した。

| tau方式 | Target 0–10M | Policy 0–10M | Target 10–20M | Policy 10–20M |
|---|---:|---:|---:|---:|
| random Run 1 | 637 | 614 | 905 | 842 |
| random Run 2 | 654 | 635 | 913 | 869 |
| **stratified** | **721** | 687 | **983** | **921** |
| **antithetic** | 712 | **704** | 965 | 892 |
| systematic | 676 | 660 | 910 | 856 |
| fixed | 645 | 632 | 730 | 724 |
| QR51参考 | 787 | 740 | 927 | 890 |

stratifiedはrandom 2本平均に対して10–20MのTargetで約+8.1%、Policyで約+7.7%。antitheticも約+6.2% / +4.3%だった。systematicはrandom平均とほぼ同じで、被覆を強くするだけでは改善しなかった。fixedは0–10Mこそrandom並みだが、10M以降の伸びが止まり明確に劣後した。

15–20MではstratifiedがTarget / Policy `1002 / 944`でreward首位、antitheticが`985 / 940`でほぼ同等だった。MaxRankはantitheticのEval1 / Eval2 `9.806 / 9.689`がstratifiedの`9.740 / 9.602`を上回った。両方式の10–20M Policy差29はrandom 2本の差27と同程度であり、単一Runから順位を確定しない。

### IQN・tail診断

10–20M平均。Eval action系はlocal action-stepをepisodeのexp-stepへ対応付けて集計した。

| metric | random平均 | stratified | antithetic | systematic | fixed |
|---|---:|---:|---:|---:|---:|
| Eval2 Policy margin MC ratio | 0.616 | **0.675** | 0.630 | 0.642 | **0.085** |
| Eval2 UQE/full-Q不一致 | 37.7% | **36.4%** | 37.2% | 37.1% | **81.7%** |
| first priority MC ratio | 2.90 | 2.66 | **2.80** | 2.66 | 2.85 |
| first cancellation ratio | **51.1%** | 53.8% | 52.9% | 53.5% | 52.2% |
| upper-tail/priority Spearman | 0.494 | 0.505 | 0.486 | 0.520 | 0.526 |
| quantile crossing ratio | 30.9% | 30.4% | 31.5% | **27.6%** | 29.1% |
| selected crossing depth p90 | n/a | 7.0% | 8.0% | **5.7%** | 6.7% |

stratifiedはrewardだけでなくPolicy marginの識別性が最も高く、UQE/full-Q不一致も最小だった。antitheticはfirst priority ratioとcancellationでstratifiedより良く、初回priority診断ではやや健全である。systematicはcrossing頻度・深さが最良でもreward改善へ結びつかなかったため、crossing低下だけを採用条件にしない。

fixedのcrossingは特別悪くない一方、Policy marginが極端に小さく、UQE/full-Q不一致がEval1約84.8%、Eval2約81.7%へ達した。Learnerの`[0,1]` fixed midpointと、UQE tail meanが`[tau_min,1]`へ写像するquery位置は`tau_min>0`で一致しない。固定点だけを学習したIQNがtail query位置へ十分に補間できず、risk scoreの行動識別に失敗した可能性と整合する。QRは固定51出力からtailを抽出し、tau入力の点間補間を必要としないため、今回のfixed IQN失敗をQRのfixed評価上振れとは解釈しない。

### 学習・PER・終端・実時間

全方式でgradient clipはほぼ0、priority clipは0、IS ESSは約0.969–0.971、初回sample countは約181、stale priorityは極小だった。Loss、TD、Qに発散はなく、方式比較を無効にする学習・PER破綻はない。20Mでは全方式ともDouble Suika未達で、長期成果の比較材料にはならない。

stratifiedの10–20M priority ESSは0.343、antitheticは0.354で、antitheticがやや分散している。Eval2のSpawnBlocked / timeoutはstratified 86.7% / 5.1%、antithetic 93.4% / 2.6%で、終端品質もantitheticがやや良い。stratifiedは高rewardと長いepisodeを得る一方、候補DROP失敗やtimeoutが少し多いため、再現Runでも監視する。

20M所要時間はstratified約1.62時間、antithetic約1.58時間、systematic約1.54時間、fixed約1.55時間。sampling方式による実用上重大なthroughput差はない。

### 判断

- `stratified`: 20M gate通過。rewardとPolicy識別性の首位候補。
- `antithetic`: 20M gate通過。MaxRank、初回priority、PER ESS、終端品質の首位候補。
- `systematic`: random同等に留まり、次段階へ進めない。
- `fixed`: 10M以降の停滞とUQE/full-Q不一致から棄却する。QR寄せIQNとしても採用しない。
- 次はstratified / antitheticを同じ新seedで各20M再実行する。結果と採用判断は探索ブロック08へ記録する。

## 探索ブロック 08: stratified / antitheticのseed 2再現と50M比較

**記録時点:** 2026-08-13、両Run正常close後

**状態:** completed / stratifiedを採用、antitheticは代替候補

**確度:** medium。20Mは各2 seed、50M比較はseed 2のみ。backendはnon-deterministic

### 実効設定とRun状態

両RunはB256/RR1、K=N=M=32、seed 2を共有し、train Policy / learner current / learner targetのsample modeだけが異なる。Eval Policy / full queryはfixed×32を維持した。

- stratified: `run_20260813-111617_dm_iqn32-stratified-b256-rr100`
- antithetic: `run_20260813-163349_dm_iqn32-antithetic-b256-rr100`

両Runとも約50Mで正常AutoPause後、852,107,692 bytesの`agent_close.anet`を保存し、`stderr.log`は空だった。

### 2-seedの20M集約

seed 1・2のRun平均。raw Eval rewardとMaxRankを同一exp-step windowで集計した。

| 方式 | Target 0–20M | Policy 0–20M | Eval1 MaxRank | Eval2 MaxRank |
|---|---:|---:|---:|---:|
| stratified | **853.1** | 797.2 | **9.519** | 9.392 |
| antithetic | 841.0 | **800.3** | 9.493 | **9.426** |

Targetはstratifiedが約1.4%、Policyはantitheticが約0.4%高く、MaxRankもEval系統で勝者が分かれた。両方式ともseed 2でseed 1の20M改善を再現したが、方式間の優劣は実質同率である。

### seed 2の50M結果

| 方式 | Target 20–50M | Policy 20–50M | Target 40–50M | Policy 40–50M |
|---|---:|---:|---:|---:|
| random 2本平均 | 1135 | 1061 | 1242 | 1151 |
| stratified | 1192 | 1113 | 1271 | 1199 |
| antithetic | **1197** | **1126** | **1296** | **1205** |
| QR51参考 | 1185 | 1127 | 1380 | 1296 |

antitheticは40–50MでstratifiedをTarget +1.9%、Policy +0.5%上回った。ただし同じEval stepで対応付けたantithetic−stratified差は、Target `+24.6`、95%区間`[-22.2,+71.4]`、Policy `+5.6`、95%区間`[-44.2,+55.4]`で、どちらも0をまたぐ。50Mでも方式差を確定できない。

両方式ともrandomを上回ったが、40–50MではQR51へTarget / Policyとも約6–8%届かなかった。Train / EvalのDouble Suikaは両方式とも未観測だった。

### 診断上のトレードオフ

seed 2、40–50M平均。

| metric | stratified | antithetic | 読み方 |
|---|---:|---:|---|
| Eval2 Policy margin MC ratio | **0.717** | 0.651 | stratifiedはrisk score差が大きい |
| Eval2 UQE/full-Q不一致 | 38.0% | **34.7%** | antitheticは期待値argmaxとの整合が高い |
| priority MC ratio | 2.83 | **2.91** | 両方十分1超、antitheticが少し高い |
| first cancellation ratio | 52.56% | **52.43%** | 実質同じ |
| priority ESS | 0.356 | **0.362** | antitheticがわずかに分散 |
| quantile crossing ratio | **26.7%** | 28.9% | stratifiedが低い |
| selected crossing depth p90 | **5.7%** | 6.6% | stratifiedが浅い |

Loss、TD、Q、gradient、PERはいずれも安定し、clip張り付きやcollapseはない。stratifiedはmarginとcrossing、antitheticはfull-Q整合性とPER診断でやや良いが、採否を反転させる差ではない。

### 採用判断

- sampling方式比較は同率として終了し、次の探索では`stratified`を標準とする。
- 理由は2-seed Targetがわずかに高いこと、全stratum被覆を保証すること、rare transitionへ外れtau集合だけが当たる問題を直接緩和することである。
- `antithetic`は性能劣後として棄却せず、stratifiedの後続探索が不安定な場合の代替候補として保持する。
- 方式差の追加seedへ予算を使わず、次はtarget tau本数`M`へ進む。

## 探索ブロック 09: tau本数探索の段取り

**状態:** planned / 次回はtarget本数M

### 順序と分離する役割

| 順序 | 軸 | 役割 | 主な計算量・注意 |
|---:|---|---|---|
| 1 | target `M` | bootstrap target分布とtarget平均の有限本数精度 | target forwardと`B×N×M` pairwise loss。lossはM方向を平均するため、期待lossスケールは直接変えない |
| 2 | train Policy `K` | UQE tail score、探索行動、Actor forwardの積分解像度 | learner lossと独立。Evalはfixed K=32を維持し、共通の物差しで比較する |
| 3 | current `N` | 勾配側quantile回帰点とcurrent分布の有限本数精度 | current forward/backwardと`B×N×M`。現契約はN方向をsumするため、N変更はraw loss・gradの実効scaleも変える |

Mはtarget精度と計算量を動かしつつ、Nのように期待lossを本数比例で変えないため最初に探索する。Kは方策側だけを切り分け、その後にNを扱う。N≠32を採用する場合は、tau数決定後に学習率との相互作用を独立に再確認する。

### Phase M: target本数

共通固定値は`sample_mode=stratified`、K=32、N=32、B256/RR1、seed 2、その他は50M stratified基準と同じとする。

| パターン | K | N | M | 目的 | 初期扱い |
|---|---:|---:|---:|---|---|
| M16 | 32 | 32 | 16 | target / pairwise計算を半減し、品質を保てるか | 実行 |
| M32 control | 32 | 32 | 32 | 採用基準 | 品質はseed 2の50M既存Runを再利用。速度だけ同じlauncherで短時間再測定 |
| M64 | 32 | 32 | 64 | target平均とbootstrap分布の有限本数誤差を下げる | 実行 |
| M8 lower bound | 32 | 32 | 8 | 計算量下限と不安定化境界 | 実行。主候補より高リスクのため最後に流す |

推奨Run名は`dm_iqn-k32-n32-m16-stratified-s2-b256-rr100`のようにK/N/M、mode、seedを明記し、省略名を使わない。

### Phase MのRun予算とgate

1. M32 / M16 / M64 / M8を同じbatch launcherで順に実行し、実効設定、shape、NaN、throughputを確認する。既存M32 50Mはonline起動だったため、batch起動候補との速度比較には直接使わない。
2. 4パターンを同じ終了stepまで実行し、0–10M / 10–20M windowを比較する。
3. M16は10–20MのTarget / PolicyがともにM32比-3%以内で、matched smokeのthroughputがM32比+5%以上なら通過候補とする。
4. M64はTarget / Policyの一方がM32比+3%以上か、MaxRankとpriority識別が揃って改善し、throughput低下が20%以内なら通過候補とする。`target_mc_scale`低下はM増加から機械的に期待されるため、単独では通過理由にしない。
5. M8はM16と同じ品質gateを満たし、さらに有意な速度利得がある場合だけ採用候補に残す。LunarLanderでの不安定化観測があるため、scoreまたは診断悪化時は即棄却する。
6. 明確な勝者を1方式だけ50Mへ延長する。差が小さければ計算量の少ない候補、品質差が確認できれば高品質側を採る。
7. 20M差が非決定論幅と同程度なら、M32を再実行せず上位候補だけseed 3で確認する。

主評価はEval Target / Policy reward、MaxRank、fruit count、episode step、終端理由。機構診断は`target_mc_scale`、`priority_mc_ratio`、`first_priority_mc_ratio`、`first_cancellation_ratio`、`first_quantile_loss_norm`、Q / TD / loss / grad、PER ESS / coverage、throughputを使う。stratifiedの`target_mc_scale`はforward間乱数分散の実測ではなく積分解像度proxyとして読む。

### 後続Phase K / Nの候補

| Phase | 初期候補 | 固定する値 | 任意の下限確認 |
|---|---|---|---|
| K | 16 / **32** / 64 | N=32、MはPhase M採用値、Eval K=32 fixed | K=8 |
| N | 16 / **32** / 64 | K / Mは採用値 | N=8 |

KではPolicy margin、UQE/full-Q不一致、報酬、Actor側throughputを重視する。Nでは`current_mc_scale`とscoreだけでなく、N方向sumによるraw loss / grad scale変化を必ず併記し、`first_quantile_loss_norm`で本数差を除いた診断も見る。各Phaseともcontrolは既存採用Runを再利用し、変更候補だけ20M gate、勝者だけ50Mとする。

## 残件と実験キュー

### Completed: B256/RR1 QR51 / IQN32 baseline pair

- 同一B256/RR1のQR51とIQN32 randomを100M、正常closeまで完了した。
- QRは終盤reward、max rank、throughputでIQNを上回り、過去QRと同等scoreを維持した。
- 両者ともEval Double Suika未達で、QRには高いSpawnBlocked、両者にはRR1のReplay coverageコストが残る。
- 200Mへ延長せず、tau sampling方式のscreeningへ移る。

### Completed: IQN32 random 基準

- `run_20260809-033908_dm_iqn32-b128-rr125`は100Mで正常AutoPause・正常closeした。
- 終盤に弱い改善とQR差縮小はあったが、Double Suika未達と約16%のreward差が残るため200Mへ延長しない。
- 保存checkpointは将来の長期追試に利用できるが、次のstratified controlはtau samplingを一軸比較するため最初から開始する。

### Completed: IQN tau sampling 5方式screening

- random / stratified / systematic / antithetic / fixedをB256/RR1、N/M/K=32の20M同一契約で比較した。
- stratifiedとantitheticはrandomを上回ってgate通過、systematicはrandom同等、fixedは明確劣後した。
- seed 2再現と50M比較でもstratified / antitheticは実質同率だった。次の標準はstratified、antitheticは代替候補とする。

### P1: target tau本数M

1. K=N=32、stratifiedを固定し、M32 / M16 / M64 / M8の順で同じ終了stepまで流す。
2. M32を同一launcherのcontrol、M16を高速化候補、M64をtarget精度候補、M8を高リスクな下限確認として評価する。
3. M8 / M16 / M32 / M64の4パターンから勝者だけ50Mへ進める。
4. 詳細な比較契約とgateは探索ブロック09を正本とする。

### P1: メトリクス契約修正

`51_eval1/41,42,51,52` と `52_eval2/41,42,51,52` の NOOP metric は `$exp_step`を指定しておらず、他の主要グラフとstep軸が一致しない。次の長時間Run前に設定を修正し、過去Runは `train_step * num_envs` で換算する。

追加すると有効な診断:

- full-range mean Q と risk-tail Q の NOOP-vs-DROP marginを分離
- `no_drop_timeout_ratio`帯ごとのNOOP margin、TD error、PER priority
- Replay sample中のNOOP比率と連続NOOP長
- Qバブル区間のepisode自動保存・事後調査

最後の項目は `docs/memo/999_episode_forensics_10prd.md` の対象とする。

### P2: Policy K / current Nとrisk policy

- M決定後にK16 / K64をK32 controlへ比較し、train Policyの積分解像度とActor計算量を決める。
- K決定後にN16 / N64をN32 controlへ比較する。N方向sumでraw loss / grad scaleも変わるため、必要ならtau数決定後にalphaを再確認する。
- train tail tau / tail meanはK/N/M決定後に一軸で扱う。learner本数、policy本数、batch/RRを同時に変えない。
- `use_optimistic_target=true`: 過去に成績改善が乏しく不安定化懸念があるため、現時点ではfalseを維持する。

### P2: NEETへの介入

- 現 RunのNEET/Qバブルは自力で鎮火したため、`noop_penalty` / `time_penalty`を基準Runへ直ちに導入しない。
- fixed-gridでも持続的Eval NEETまたはQ再膨張が再現した場合、small `noop_penalty`を一軸A/Bする。
- n-step増加は遅延罰伝播に効き得るが、DropMerge過去結果で局所解懸念があるため、現在の第一候補にはしない。

### P2: QR長期lineage側の未解決

- [長期 Run: batch / replay 探索](2026-07-27_longrun-batch-replay.md)のB512/RR1診断は未実行。batch効果とsample budget効果の分離課題として維持する。
- 保存済みQR lineageの終点はcy07。cy08 B512/RR2は改善余地を示したが異常終了し、checkpointを保存できていない。
- IQN基礎比較中はbatch/RR探索を混ぜない。IQN採否後に、採用mode上で改めて実時間効率を測る。

### P3: 長期基盤・後続アルゴリズム

- `docs/memo/999_serialize_10prd.txt`: Agentだけでなくoptimizer、ReplayBuffer、Env、step、schedulerを含むRun全体save/load契約。checkpoint継続時のalpha等を正しく制御する前提基盤。
- `per_initial_priority_mode=actor_approx`: 実装済みだが長期主力はfixed 0.3を維持。IQN基礎比較後の独立課題。
- BBF / BTR / multi-step系: 現IQN/QR差とQバブル機構を確定してから、次世代長期Runのsample efficiency改善として扱う。

## 継続・棄却基準

| 対象 | 継続条件 | 棄却・保留条件 |
|---|---|---|
| IQN32 random B128/RR1.25 | **100Mで完了**。弱い正の傾きとQR差縮小は観測 | DS 0と約16%差が残るため200M延長は保留 |
| QR51 B256/RR1 | **100Mで完了・高速baseline採用**。過去QR同等scoreと高throughput | 200Mへ延長せずsampling探索を優先 |
| IQN32 stratified | **採用**。2-seed再現と50M screening完了 | 後続tau数探索の標準mode |
| IQN32 antithetic | stratified同等の代替候補として保持 | 追加seedは行わず、stratifiedが後続で不安定な場合だけ再検討 |
| IQN32 systematic | crossingは浅いがrewardはrandom同等 | **20Mで棄却。延長しない** |
| IQN32 fixed | Policy margin 0.085、UQE/full-Q不一致約82% | **20Mで棄却。延長しない** |
| QR32 formal A/B | IQN stratified / fixedが有望で、algorithm差の確証が必要 | IQN controlが早期に明確劣後なら後回し |
| M16 | M32比score -3%以内かつ実時間性能が明確に改善 | 品質低下が速度利得に見合わなければ棄却 |
| M64 | score / MaxRank / target診断が実質改善し、throughput低下20%以内 | 改善なし、または計算費用が大きければ棄却 |
| noop penalty | Evalで持続的NEETが残る | 一時的スランプが自力鎮火する限り保留 |

## 関連文書

- [DefaultDQNAgent共通知見](../README.md)
- [DropMerge長期Run batch / replay探索](2026-07-27_longrun-batch-replay.md)
- [LunarLander IQN導入とQR比較](../lunarlander/2026-08-08_iqn.md)
- [PRD039 NoLegal adjudication](../../../memo/039_dropmerge_nolegal_adjudication_10prd.md)
- [PRD040 prev_action observation](../../../memo/040_dropmerge_prev_action_obs_10prd.md)
- [PRD044 IQN stratified tau sampling](../../../memo/044_iqn_tau_stratified_sampling_10prd.md)
- [PRD047 IQN探索P0診断メトリクス](../../../memo/047_iqn_search_p0_metrics_10prd.md)
- [PRD048 分位tail探索診断メトリクス](../../../memo/048_quantile_tail_metrics_10prd.md)
- [暫定PRD999 episode forensics](../../../memo/999_episode_forensics_10prd.md)
- [暫定PRD999 Run serialize](../../../memo/999_serialize_10prd.txt)
